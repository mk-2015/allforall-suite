# Vector3::Cross

```cpp
err Cross(const Vector3& b, Vector3& out) const;
```

Calculates the cross product of the current vector and vector `b`, storing the result in `out`.

### Parameters
- `b`: Const reference to second `Vector3`.
- `out`: Destination `Vector3`.

### Returns
- `EV3_SUCCESS` on success.
