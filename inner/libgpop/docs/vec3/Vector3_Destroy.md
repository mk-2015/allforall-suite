# Vector3_Destroy

```c
err Vector3_Destroy(Vector3** Vector);
```

Safely deallocates a `Vector3` instance and clears its pointer to `NULL`.

### Parameters
- `Vector`: Double-pointer to the `Vector3` to destroy.

### Returns
`EV3_SUCCESS` on success.
