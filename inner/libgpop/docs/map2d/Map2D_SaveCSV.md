# Map2D_SaveCSV

```c
err Map2D_SaveCSV(Map2D* Map, const char* filename);
```

Saves the map to a CSV file.

### Parameters
- `Map`: Pointer to `Map2D`.
- `filename`: Path to the CSV file.

### Returns
`MAP2D_SUCCESS` on success or an error code.
