# Map3D_GetByte

```c
void Map3D_GetByte(Map3D* Map, uint8_t* out);
```

Retrieves the currently selected byte and stores it in the `out` parameter.

### Parameters
- `Map`: Pointer to the `Map3D` structure.
- `out`: Pointer to a byte where the result will be stored.
