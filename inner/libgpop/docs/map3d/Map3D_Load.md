# Map3D_Load

```c
Map3D* Map3D_Load(const char* filename);
```

Loads 3D map data from a file.

### Parameters
- `filename`: Path to the file to load from.

### Returns
Returns a pointer to a newly allocated `Map3D` structure, or `NULL` on failure.
