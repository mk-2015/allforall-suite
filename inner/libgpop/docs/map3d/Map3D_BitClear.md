# Map3D_BitClear

```c
void Map3D_BitClear(Map3D* Map, int8_t n);
```

Clears (sets to 0) the n-th bit (0-7) of the currently selected byte.

### Parameters
- `Map`: Pointer to the `Map3D` structure.
- `n`: Bit index (0-7).
