# Map3D_GetLastError

```c
err Map3D_GetLastError(Map3D* Map);
```

Retrieves the last error code encountered by the 3D Map operations.

### Parameters
- `Map`: Pointer to the `Map3D` structure.

### Returns
The last error code (`err`). Returns `MAP3D_SUCCESS` if no error occurred.
