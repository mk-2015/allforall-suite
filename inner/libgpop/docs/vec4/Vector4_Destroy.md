# Vector4_Destroy

```c
err Vector4_Destroy(Vector4** Vector);
```

Safely deallocates a `Vector4` instance and clears its pointer to `NULL`.

### Parameters
- `Vector`: Double-pointer to the `Vector4` to destroy.

### Returns
`EV4_SUCCESS` on success.
