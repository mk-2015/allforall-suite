# Vector2::Rotate

```cpp
err Rotate(double angle_rad);
```

Rotates the current vector by the specified angle in radians.

### Parameters
- `angle_rad`: Angle to rotate in radians.

### Returns
- `EV2_SUCCESS` on success.
- Error code on failure (e.g., overflow).
